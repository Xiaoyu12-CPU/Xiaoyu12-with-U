# 美术制作与状态接入规则

适用于本项目后续桌宠表情、动作差分、白边清理和动画状态替换。用户当次明确要求优先于以下默认规则。资源编号与路径见 [ART_ASSETS.md](ART_ASSETS.md)。

## 固定流程

`指定参考与变化 → 制作主表情 → 局部修正 → 200×200 优化与验收 → 制作动作差分/预览 → 按要求接入状态 → 验证运行版本 → 更新索引`

用户仅要求生成时，交付新增候选和预览。用户明确要求替换、接入某状态，或已经授权这些步骤时，完成验收后直接执行，不重复索要确认。不根据表情名称擅自选择状态，例如闭眼笑不自动对应 happy、慌张不自动对应 alert。

## 1. 参考与编号

- 每次先读本规则和资源索引，只打开本次需要的参考图，不重复扫描全仓库、重读历史提示词或批量载入无关图片。
- 新表情默认以清理后的 A01 为角色基准；用户指定 A04 等参考时，以指定图为准。动作差分以已确认的主表情为准。
- 新的独立画面顺序编号 A06、A07……；同一画面的清理、压缩、修错沿用编号。A 编号是画面标识，不等于动画帧序号或状态名。
- 明确左右：角色自身右侧对应画面左侧。存在朝向变化时根据实际姿态说明，不能机械套用。

## 2. 画面不变量与修改范围

- 固定角色身份、发型、皇冠、眼罩、兔子装饰、发带、服装、配色和像素风格；只改本次指定内容。
- 局部表情修正、单像素修补、白边清理，只合入经过核查的目标区域，保留其余原像素。生成工具输出不能未经比较就整图覆盖。
- 仅要求位移的差分，最终按整数像素平移，保持人物尺寸、表情、颜色及 alpha；人物和头顶符号可独立移动。不得产生缩放、拉伸或脸部重绘。
- 存在固定前景物体时，单独锁定其全部原像素，人物在后层移动后再覆盖前景；从当前成图分离可见人物，不从早期参考图恢复已经被物体遮挡的躯干。
- 以像素记录位移。A05 的例子：人物相对 A04 下移 3 px，符号相对 A04 右移 2 px、上移 1 px。先检查四边余量，避免裁切。
- 使用当前可用的内置图像编辑流程；工具选择遵守当次工具要求。模型输出只是候选，最终以这些画面约束和逐像素核查为准。

## 3. 交付规格与白边检查

- 正式表情统一为 **200×200、RGBA PNG、真实透明背景**。不把棋盘格或纯色背景画进 PNG。
- 较大生成图使用最近邻缩到目标画布，保持方形构图和对齐；以实际解码尺寸验收，不能只信提示词、文件名或历史配置。
- 清理外轮廓和发丝透明缝隙中的白色/中性浅灰残留；保留皇冠、兔子、眼睛、衣领及正常灰紫高光。不能全局删除白色或把所有浅色像素变透明。
- 分别在深色、浅色背景检查；同时看原始 1 倍效果和最近邻放大效果。确认无白边、破洞、错位、额外轮廓和非目标区域变化。
- 保留有效 alpha，清零完全透明像素中的隐藏 RGB，并做无损 PNG 压缩；记录真实字节数。不要为追求文件大小损坏画面。
- 工程使用优化后的正式图；未采用的大尺寸候选留在生成输出或临时目录，不加入运行时资源目录。

## 4. 动画与状态接入

- 动画明确记录：目标状态、A 编号顺序、每帧毫秒数、是否循环、循环间隔模式。状态绑定与表情制作分开描述。
- 未指定快速抖动的时长时，可沿用本次 `120 / 120 / 160 ms` 作为预览起点，按实际观感调整并说明；不能将这一节奏强加给睡眠等慢动作。
- 连续循环使用 `loop: true`、`replay.mode: continuous`。首尾相同画面跨轮会连续停留，例如当前 tired 的 A04 为尾帧 160 ms 加下一轮首帧 120 ms。
- 工作图及制作记录在 `art/pets/default/`；接入用图在 `src/assets/pets/default/<state>/`，顺序和时长配置在 `src/assets/pets/default/pet.json`。
- 当前加载器按路径生成帧 ID，编辑器按 ID 渲染：重复画面需独立帧路径。当前 tired 为 `panicked-001=A04、002=A05、003=A04`；同内容构建产物可去重。
- 检查本机该状态的用户覆盖，避免它遮住新内置配置。修改仅限指定状态，保留其他状态和用户动画参数。
- 修正某张正式图时，同步它的已接入副本和使用它的预览。例如 A01 与 normal-004、A04 与 tired / dragging 第 1/3 帧；不要留下旧画面副本。

## 5. 运行验证与记录

- 验证文件存在、实际尺寸/alpha、源图与接入副本一致、帧路径唯一、顺序及时长正确。修改后的动画需要预览核查。
- 编辑器从图片读取 `naturalWidth/naturalHeight`，尺寸刷新不应制造未保存修改，也不改变时长或顺序。
- 接入后运行构建；代码变化执行相关现有测试。核对开发服务返回的新文件/配置，避免只验证磁盘文件。
- 启动查看时辨认源码开发进程与已安装旧版；不得把旧安装版窗口当作最新修改的运行结果。
- 更新资源索引的编号、文件路径、尺寸、体积和状态映射；完整提示词放制作记录。回复简要说明结果、修改范围及验证情况。

## 本次已落地的例子

- A01：默认睁眼；A02：半闭眼过渡；A03：闭眼笑。三张已统一 200×200 并清理白边。
- A04：基于 A01 的慌张脸；嘴巴左上角突起已修平。
- A05：基于 A04 的下移动作差分，保持 A04 表情原像素。
- tired：`A04 (120 ms) → A05 (120 ms) → A04 (160 ms)`，400 ms 连续循环，不包含 A01。早期 `A01→A04→A05→A04` GIF 仅为转场预览。
- dragging：`A04 (120 ms) → A05 (120 ms) → A04 (160 ms)`，400 ms 连续循环；内置和本机用户配置一致。运行文件为 `dragging/panicked-001=A04、002=A05、003=A04`，使用独立运行副本，原 tired 映射和配置不变。实际循环不包含 A01，沿用上述 GIF 作为转场预览。
- alert：`A06 (120 ms) → A07 (120 ms) → A06 (160 ms)`，400 ms 连续循环。
- working：`A10 (120 ms) → A11 (120 ms) → A10 (160 ms)`，400 ms 连续循环；内置和本机用户配置一致。运行文件为 `working/laptop-001=A10、002=A11、003=A10`，首末帧使用独立路径。旧 `working/attention-001.png` 至 `attention-004.png` 保留供旧配置兼容，当前不再使用；A06/A07 当前仅供 alert，alert 配置不变。
- A08：稍扁的 `><` 笑眼、眉内侧略下倾、倒三角嘴与金色 ✨；A09 保留全部原像素，人物下移 3 px，三颗 ✨ 整体右移 2 px、上移 1 px。
- happy：`A08 (120 ms) → A09 (120 ms) → A08 (160 ms)`，400 ms 连续循环；内置和本机用户覆盖一致。运行文件为 `happy/happy-001=A08、002=A09、003=A08`。原 `A01→A08→A09→A08` GIF 保留为转场预览，不代表 happy 的实际帧序列。
- A10：基于 A06，仅在下方加入银白笔记本背面；保留脸部和领口。后续按用户要求将笔记本宽高各放大 30%，取整为 116 × 27 px，底边不变；居中小熊 logo 参考额头头饰，现为纯灰色，并随背板平面压扁、轻微向下收窄。已接入 working 第 1、3 帧。
- A11：A10 的可见人物下移 3 px，感叹号右移 2 px、上移 1 px；电脑与标志的全部 3,044 个原像素固定。已接入 working 第 2 帧。A01 → A10 → A11 → A10 的 GIF 保留为转场预览，500 / 120 / 120 / 160 ms、无限循环；实际 working 循环不包含 A01。

## 后续简短指令模板

> 按 ART_WORKFLOW.md，以 A__ 为参考制作 A__：【表情/局部变化】。差分：【位移/符号变化，可省略】。接入：【状态、顺序、时长、循环方式；仅生成时省略】。

未说明的画面部分默认保持原样，200×200、真实透明、白边检查和文件压缩无需每次重复提出。
