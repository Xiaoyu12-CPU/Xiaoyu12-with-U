# 变更记录

本文件记录公开版本的用户可见变化，并说明不同长期分支在发布时如何收敛。完整提交历史仍以 Git 为准。

## v0.5.1 — Windows x64 专项修复

- 修复 Windows 原生窗口拖动接管鼠标消息后，普通点击丢失 DOM `click`、桌宠未进入 `happy` 的问题：未发生移动的松键兜底补触发一次点击。
- 同次按下产生的迟到 DOM 点击会被去重；新的连续点击继续响应，真实拖拽和取消操作不会误触发开心，macOS 原有处理路径保持不变。
- 新增 6 组回归场景：原生松键补点击、迟到 DOM 点击去重、迟到原生松键去重、连续点击、真实拖拽和取消后的松键。Windows 实机验证尚未完成。
- 沿用 v0.5.0 的 A01–A11 资源、默认动画和设置，不新增资源或设置迁移；本版仅发布新的 Windows x64 安装程序，macOS 安装包继续使用 v0.5.0。
- 源码版本统一为 0.5.1，发布时将 `main` 与 `develop` 收敛到同一提交并新建 `v0.5.1` 标签；构建与发布附件的实际结果须按发布流程核查。

## v0.5.0

### 表情资源与默认动画

- 正式美术资源整理为 A01–A11：A01 默认、A02 过渡、A03 闭眼笑、A04/A05 慌张、A06/A07 注意、A08/A09 开心，以及带银白笔记本的 A10/A11 工作表情。
- 正式表情统一为 200×200 RGBA 透明 PNG，优化图片体积并清理 A01/A02/A03 的发丝白边；动作差分保留原表情像素，人物下移 3 px，头顶符号右移 2 px、上移 1 px。
- 工作差分只移动 A10 当前可见的人物，笔记本和灰色小熊标志的全部原像素固定，避免电脑底部出现原本被遮住的身体细条。
- idle 内置动画为 A01 → A02 → A03 → A01，时长 200 / 100 / 200 / 0 ms；末帧保持，随机等待 10 / 3 / 30 / 22 秒后重播。
- happy 使用 A08 → A09 → A08；tired 和 dragging 使用 A04 → A05 → A04；alert 使用 A06 → A07 → A06；working 使用 A10 → A11 → A10。上述三帧动画均为 120 / 120 / 160 ms、400 ms 连续循环。sleep 继续使用原占位配置。
- 动画重复画面使用独立帧路径，旧占位资源和 working 的旧 attention 帧保留供已保存配置兼容。含 A01 的 GIF 仅作表情转场预览，不加入这些状态的三帧循环。

### 动画编辑器与主题

- 修复动画编辑器沿用旧尺寸缓存的问题：从 PNG 重新读取实际宽高，200×200 图片不再显示为旧的 1254×1254，尺寸读取也不会制造未保存修改。
- 整合 v0.4.6 / v0.4.6.1 的主题功能：默认、蜜柑和自定义主题分别保存完整外观，新增主题从基础默认外观开始，背景、透明度、模糊和文字阴影随主题正确切换。
- 保留已有主题兼容处理。此次资源与默认值更新不新增设置或用户资源迁移；已有自定义动画、主题、输入与监控选项、提醒、对话和窗口布局继续优先，不被新内置值强制覆盖。

### 发布基线

- 应用版本目标统一为 0.5.0，发布时将 main 与 develop 收敛到同一发布提交，并使用新标签 v0.5.0；旧标签保留为历史快照。
- 本条目描述版本内容；构建、三平台运行验收与发布附件的实际结果按发布流程核查，不由版本条目推定完成。

## v0.4.6.1 — 历史开发基线（已纳入 v0.5.0）

### Theme 数据模型修正

- 默认主题、蜜柑主题与自定义主题现在都是独立、完整且可编辑的控制中心外观实体；对颜色、透明度、背景模糊、文字阴影、侧栏和卡片的修改会写回当前主题。
- 切换主题时直接恢复目标主题保存的完整外观，顶层 `controlCenter` 只保留为当前主题的运行时/兼容镜像，避免主题名称与实际背景不一致。
- “新增主题”改为依次创建“自定义主题1、2……”并立即选中；新主题使用基础默认 token、没有背景图片，也不会复制当前主题。
- 背景引用按主题保存。替换或移除背景时，如果同一托管图片仍被其他主题使用，不会提前删除该文件。
- 为 v0.4.6 的旧 `settings.json` 增加最小迁移：原有背景、颜色、透明度、文字阴影和其他控制中心外观字段会迁入匹配的内置主题或新的自定义主题，设置 schema 继续保持 `1`。
- 用户可继续编辑默认主题和蜜柑主题，与 v0.4.6 已有交互保持一致；“恢复默认视觉”只还原默认主题并切换到它，不改变其他主题。

## v0.4.6 — 历史开发基线（已纳入 v0.5.0）

### 控制中心外观主题

- 在“控制中心外观”的背景卡片上方增加横向主题选择器，支持鼠标拖动、触控板滚动、可见横向滚动条与键盘访问。
- 保留 v0.4.5 的控制中心默认设置为“默认主题 / 外观主题 1”。
- 将开发机当前确认并压缩为 1280×720 的背景图及完整外观参数固化为“蜜柑主题 / 外观主题 2”；优化后的图片作为内置资源随应用发布，不依赖本机 app data。
- 增加“新增主题 / 空白主题”，以无背景图片的干净外观作为继续自定义的起点。
- 主题切换只替换 `controlCenter` 外观字段，不影响桌宠、桌面浮层、提醒、输入监听、对话和窗口位置。

### 图片与文字可读性

- 背景图片新增 `0–30 px` 模糊度设置；滤镜只应用于独立背景图片层，并扩展图层边界避免模糊后的边缘露底。
- 内容区文字新增阴影颜色、`0–100%` 透明度、`0–8 px` 大小和 `0–30 px` 模糊范围设置；默认关闭，旧设置自动补默认值。

## v0.4.5.2 — 2026-09-01

### Windows x64 拖动修复

- 修复 Windows 原生窗口拖动接管鼠标消息后，WebView 偶发收不到 `pointerup / mouseup`，导致 `interaction.drag` 请求未释放、桌宠永久显示“拖动中”的问题。
- 增加 Windows 主鼠标按键状态探测作为结束兜底；它只判断按键是否仍被按住，不记录鼠标位置、轨迹或所处应用。
- 保留 macOS 现有原生拖动和 `pointercancel` 时序，非 Windows 平台不会启用轮询。
- 本版本只提供新的 Windows x64 安装程序，macOS 安装包继续使用 v0.4.5.1。

## v0.4.5.1 — 2026-09-01

### 鼠标可视化窗口

- 移除鼠标图案上方的三段虚线拖动提示。
- 将窗口拖动热区迁移到鼠标图案的背部，空白区域不再承担主要拖动操作；鼠标按键、滚轮和侧键的状态显示保持不变。

## v0.4.5 — 2026-09-01

### 控制中心与语言

- 在控制中心左下角加入中文、English、日本語选择器，语言偏好写入统一设置并在下次启动时恢复。
- 为控制中心页面、设置页、编辑器、状态页和桌面浮层的固定界面文字加入中英日翻译。
- 左上角品牌标记由“12”文字块替换为正式应用图标，品牌副标题更新为 `v0.4.5`。

### 默认外观与窗口位置

- 将当前确认的桌宠缩放、系统状态窗口外观、键盘历史外观、鼠标可视化外观和控制中心主题固化为 v0.4.5 新安装默认值。
- 将三个浮层相对桌宠的位置固化为：系统状态 `(19, 152)`、键盘历史 `(135, 33)`、鼠标可视化 `(171, 201)`（逻辑像素）。
- 新增桌宠主窗口位置保存与启动恢复；三个浮层继续保存绝对位置和相对桌宠位置，跟随时不累积移动误差。
- 保存位置若已不属于当前任何显示器，则回退到主显示器左下角的安全默认位置，避免更换显示器后找不到桌宠。
- 首次安装和恢复全部默认设置时仍只显示桌宠，不自动开启系统状态、键盘历史、鼠标可视化、系统监控或键鼠监听。
- 修复桌宠右键菜单在窗口右侧或底部被裁切的问题；菜单会按实际尺寸自动保持在可见区域内。

### 兼容与验证

- 旧版缺少语言字段的 `settings.json` 自动补为中文；已有用户设置和已保存窗口位置继续优先，不会被新默认值强制覆盖。
- 增加语言选择、翻译占位符、v0.4.5 品牌信息、默认相对位置和桌宠位置命令的回归检查。

## v0.4.4 — 2026-09-01

### 相比 v0.4.3-preview

#### 控制中心与设置

- 删除状态页、桌宠右键菜单中的开发测试、行为请求和动画暂停入口。
- 合并键盘历史和鼠标可视化的重复显示开关，避免“功能已开但窗口没开”的冲突状态。
- 将三个浮层共用的“跟随桌宠”移动到通用设置；各窗口设置页只保留自身显示、穿透、位置与外观。
- 状态页为未启用或缺少权限的系统监控、键盘、鼠标提供直接跳转到对应设置页的入口。
- 动画、对话和提醒页面增加未保存修改保护；离开页面或关闭控制中心前会确认。
- 增加恢复全部应用设置、恢复控制中心外观和重置各浮层位置的明确入口。
- 统一控制中心主题变量，使成功、警告、错误、按钮和卡片样式跟随自定义配色。
- 修正非循环动画编辑逻辑：播放一次后停在末帧，不再显示无效的 Replay Delay 设置。

#### 窗口、输入与稳定性

- 修复 macOS 修改“打字反馈 → 时间窗口”等设置时可能出现的彩虹圈死锁。
- Rust 窗口尺寸、跟随和位置命令改为异步调度，避免主线程与全局窗口锁互相等待。
- 前端只在真正影响窗口的配置变化时同步浮层；对话、提醒和打字反馈不再引发窗口重排。
- 键盘和鼠标的 macOS 权限自动重试移到桌宠主 Runtime，控制中心关闭后仍会继续恢复。
- 选择键盘或鼠标窗口方位时立即重置到新的相对锚点；显示开关与实际窗口可见状态保持一致。
- 清理已经失效的启动开发提示、旧 `displayMode` 设置和对应对话事件入口。

#### 默认行为与测试

- 首次安装继续默认只显示桌宠本体；系统状态、键盘历史、鼠标可视化、系统采样和输入监听均默认关闭。
- 增加窗口设置签名、默认可见性、权限重试归属和生产 UI 无调试入口的回归检查。
- 版本号统一更新为 `0.4.4`。

### 发布前的分支对比

| 旧分支 | 合并前最后提交 | v0.4.4 相对它的变化 |
|---|---|---|
| `main` | `3a594de`，早期系统监控与 Windows 构建 | 补齐提醒、键鼠感知、五项系统监控、控制中心自定义、多窗口桌面、三平台 Release 和本次 UI/稳定性修复 |
| `develop` | `59ae31b`，基于 v0.4.2 实验多窗口并附旧交接文档 | 用从 v0.4.1-preview.1 重建的四窗口架构取代实验实现，并加入绝对坐标跟随、原子位置保存、UI 清理和 macOS 死锁修复 |
| `codex/v0.4.3` | `3d1816d`，四窗口重建版 | 保留四窗口基线，新增本节列出的控制中心、设置语义、权限恢复和并发加固 |

发布 v0.4.4 后，`main`、`develop` 和 `codex/v0.4.4` 应指向同一个发布提交。旧的 `codex/v0.4.3` 与旧标签保留为历史快照。

## v0.4.3-preview — 2026-08-30

- 从 v0.4.1-preview.1 重新实现桌宠、系统状态、键盘历史和鼠标可视化四个独立窗口。
- 增加浮层跟随、自由拖动、点击穿透、绝对/相对位置持久化和窗口布局测试。
- 恢复 macOS ARM64、macOS Intel 和 Windows x64 三平台自动发布。

## v0.4.2-preview — 2026-08-29

- 首次尝试拆分桌面 UI 和保存浮层位置。
- 该实验版存在窗口边界、拖动、跟随和位置存储问题，后续由 v0.4.3 的重建方案取代。

## v0.4.1-preview.1 — 2026-08-29

- 实现 Windows 全局键鼠监听并加固 Hook 线程。
- 增加 macOS 输入权限自动恢复、Windows 构建诊断和失败日志分支。

## v0.4.1-preview — 2026-08-28

- 修复 Windows 控制中心白屏和死锁。
- 加固提醒、对话和输入运行时，加入 macOS 与标签驱动的自动发布流程。

## v0.4.0-preview — 2026-08-27

- 增加提醒、全局键鼠感知、打字反馈、Intel Mac 支持和控制中心外观设置。

## v0.3.0-preview — 2026-08-23

- 首个公开预览：桌宠动画与对话、控制中心、CPU/内存/网络/存储/电池状态展示基础。
